import React from 'react';
import axios from "axios"

export default class Logout extends React.Component
{
    constructor(props)
    {
        super(props)
    }

    render()
    {
        return <div>
            <div>
                <button onClick={() => {

                }
                }>退出登录
                </button>

            </div>
        </div>
    }


}